import React, { FC, useState, useEffect } from 'react';

//const Timer: FC<{ limit: number }> = ( {limit} ) => {  // 分割代入
//  const [timeLeft, setTimeLeft] = useState(limit);
//  const reset = () => setTimeLeft(limit);
//  const tick = () => setTimeLeft(t => t - 1);
//  
//  
//}